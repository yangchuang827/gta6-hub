---
import { categories, siteConfig } from '@data/articles';

const currentPath = Astro.url.pathname;
---

<header class="sticky top-0 z-50 border-b border-ink-100 bg-white/80 backdrop-blur-xl">
  <div class="container-page">
    <div class="flex h-16 items-center justify-between">
      <!-- Logo -->
      <a href="/" class="flex items-center gap-1.5">
        <span class="text-xl font-extrabold tracking-tight text-ink-900">GTA6</span>
        <span class="text-xl font-extrabold tracking-tight text-accent">Hub</span>
      </a>

      <!-- Desktop Nav -->
      <nav class="hidden lg:flex items-center gap-1">
        <a
          href="/"
          class={`px-3 py-2 rounded-lg text-sm font-medium transition-colors ${currentPath === '/' ? 'text-accent' : 'text-ink-500 hover:text-ink-900'}`}
        >
          首页
        </a>
        {categories.slice(0, 5).map((cat) => (
          <a
            href={`/category/${cat.slug}`}
            class={`px-3 py-2 rounded-lg text-sm font-medium transition-colors ${currentPath === `/category/${cat.slug}` ? 'text-accent' : 'text-ink-500 hover:text-ink-900'}`}
          >
            {cat.name}
          </a>
        ))}
      </nav>

      <!-- Right Actions -->
      <div class="flex items-center gap-3">
        <a
          href="/news"
          class="hidden sm:inline-flex items-center px-4 py-2 rounded-full text-sm font-semibold text-white bg-ink-900 hover:bg-ink-700 transition-colors"
        >
          全部资讯
        </a>

        <!-- Mobile Menu Toggle -->
        <button
          id="menu-toggle"
          class="lg:hidden p-2 rounded-lg text-ink-500 hover:text-ink-900 hover:bg-ink-50"
          aria-label="菜单"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div id="mobile-menu" class="hidden lg:hidden border-t border-ink-100 bg-white">
    <nav class="container-page py-4 flex flex-col gap-1">
      <a href="/" class="px-3 py-2 rounded-lg text-sm font-medium text-ink-500 hover:text-ink-900 hover:bg-ink-50">首页</a>
      {categories.map((cat) => (
        <a
          href={`/category/${cat.slug}`}
          class="px-3 py-2 rounded-lg text-sm font-medium text-ink-500 hover:text-ink-900 hover:bg-ink-50"
        >
          {cat.name}
        </a>
      ))}
      <a href="/news" class="px-3 py-2 rounded-lg text-sm font-semibold text-white bg-ink-900 mt-2 text-center">全部资讯</a>
    </nav>
  </div>
</header>

<script>
  const toggle = document.getElementById('menu-toggle');
  const menu = document.getElementById('mobile-menu');
  toggle?.addEventListener('click', () => {
    menu?.classList.toggle('hidden');
  });
</script>
